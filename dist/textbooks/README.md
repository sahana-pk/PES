Place subject-wise textbook files in this folder for static hosting.

Recommended layout:

- `public/textbooks/<subjectId>/<file-name>.pdf`

Then in admin, store the URL as:

- `/textbooks/<subjectId>/<file-name>.pdf`

